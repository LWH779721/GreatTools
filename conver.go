package main

import (
    "fmt"
    "strconv"
)

func main(){
    a := 12345
    
    b := strconv.Itoa(a)  
    fmt.Printf("%s, %d \n", b, a)
     
     print("hello test\n")
    //fmt.Printf("%c , %d , len: %d\n", a[0], a[0], len(a))
}